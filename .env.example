# GCP / Vertex AI
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
GOOGLE_CLOUD_PROJECT=gen-ai-poc-onboarding
GOOGLE_CLOUD_LOCATION=us-central1

# MCP Server credentials (fill in your actual values)
# Each MCP server contributes its own variables — see mcp_config.json for the full list